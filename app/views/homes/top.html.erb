<main>
  <h1>welcome to <storong>Bookers</storong> !!</h1>
  <p>in <storong>Bookers,</storong></p>
  <p>you can share and exchange your opinions , inpressions , <br>
  and emotions</p>
  <p>about various books!</p>
  <% if user_signed_in? %>
    <%= link_to "Log in" , user_path(current_user.id), class: "btn btn-info btn-sm btn-block mb-3 sign_in"%>
    <%= link_to "Sign up" , user_path(current_user.id), class: "btn btn-success btn-sm btn-block mb-3 sign_up" %>
  <% else %>
    <%= link_to "Log in" , new_user_session_path, class: "btn btn-info btn-sm btn-block mb-3 sign_in" %>
    <%= link_to "Sign up" , new_user_registration_path, class: "btn btn-success btn-sm btn-block mb-3 sign_up" %>
  <% end %>
</main>
