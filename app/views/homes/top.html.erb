<main>
  <h1>welcome to <storong>Bookers</storong> !!</h1>
  <p>in <storong>Bookers,</storong></p>
  <p>you can share and exchange your opinions , inpressions , <br>
  and emotions</p>
  <p>about various books!</p>
  <% if user_signed_in? %>
    <a class="btn"><%= link_to "Log in" , user_path(current_user.id) %> </a>
    <a class="btn"><%= link_to "Sign up" , user_path(current_user.id) %> </a>
  <% else %>
    <a class="btn"><%= link_to "Log in" , new_user_session_path %> </a>
    <a class="btn"><%= link_to "Sign up" , new_user_registration_path %> </a>
  <% end %>
</main>
